# Root pyproject.toml — Python tooling config for ai-service
# The actual project definition is in ai-service/pyproject.toml

[tool.ruff]
target-version = "py311"
line-length = 100

[tool.mypy]
python_version = "3.11"
strict = true
ignore_missing_imports = true
